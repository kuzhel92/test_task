{% extends 'base.html.twig' %}

{% block body %}
    <h1>Category creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input style="margin-bottom:10px;" class="btn btn-success" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul style="padding: 0">
        <li style="list-style-type:none;">
            <a class="btn btn-info" href="{{ path('category_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
