{% extends 'base.html.twig' %}

{% block body %}
    <h1>Manufacture</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ manufacture.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ manufacture.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('manufacture_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('manufacture_edit', { 'id': manufacture.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
